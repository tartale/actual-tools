---
name: fire-dashboard
description: "Status/design for ./actual app's Retirement section (replaced ./actual configure and ./actual reports fire) -- a FIRE dashboard built on Actual's own native dashboard widgets, first phase of a planned companion app"
metadata: 
  node_type: memory
  type: project
  modified: 2026-09-06T02:39:06.823Z
  originSessionId: 860c6192-bf02-40b0-9166-e69d50daa24b
---

`./actual report fire` and `./actual accounts classify` (2026-09-05, Phases 1-2
of a plan approved that session) build a FIRE (Financial Independence, Retire
Early) dashboard from the user's real Actual Budget data, inspired by the
feature scope of the (still unstarted, code-wise) `tartale/fire-me` repo —
see [[set-budget-migration]] for the original "possible future FIRE tool"
rationale behind writing `actual-helpers.ts` reusably in the first place.

**The core design decision, found by reading Actual's own upstream source
(actualbudget/actual on GitHub), not guessed**: Actual Budget already has
native dashboard widgets that are almost exactly a FIRE calculator —
`crossover-card` (a safe-withdrawal-rate "when can I retire" projection) and
`monte-carlo-card` (a full retirement simulator with per-account tax
treatment/access age/withdrawal strategy). So instead of reimplementing FIRE
math, this tool's job is to gather real account/spending data, classify
accounts, and **configure Actual's own already-vetted widgets** — never
touching FIRE math itself.

**No API writes a dashboard directly.** Confirmed by reading `@actual-app/api`'s
public method list (no dashboard/report methods at all) and by live-probing
this repo's existing REST wrapper (`/query`, `/aql`, `/dashboard`, `/reports`
all 404). The dashboard CRUD RPC handlers (`dashboard-create`,
`dashboard-import`, etc.) are internal-only, reachable by Actual's own client,
not external scripts. **Resolution**: the tool writes a local JSON file in
Actual's own `ExportImportDashboard` export/import shape; the user imports it
via Actual's real, currently-shipping dashboard "..." menu → Import. Zero new
API surface. **`dashboard-import` is destructive** — it replaces every widget
on the target page, no merge mode — so the tool's own help text and the
README both loudly instruct: create a dedicated, disposable page (e.g. named
"FIRE") and never import onto a page you care about. Actual's own undo
(ctrl-z) is the safety net for a bad import.

Two upstream gotchas that would have silently broken the widget if missed
(found by reading the actual crossover-spreadsheet.ts source, not assumed):
- `crossover-card`'s `incomeAccountIds` field is misleadingly named — it's
  not "accounts that receive income," it's the investable-portfolio balance
  the safe withdrawal rate is computed against. Must be exactly the accounts
  classified retirement/HSA/taxable-investment (never debt, never
  cash-other) — this is exactly what account classification exists to get
  right.
- `expenseCategoryIds` must never be left empty — an empty array silently
  zeroes the projected expense, making the widget claim "already FI" with
  $0/month spending.

**Account classification** (`src/fire-accounts.ts`): Actual's account API
exposes no type field at all (`{id, name, offbudget, closed}` only), so
classification is hybrid per the user's explicit choice — ordered
case-insensitive name-pattern heuristics (roth/hsa checked before the
broader 401k/ira pattern, so "Roth 401k" classifies correctly) with an
explicit `fire-accounts.json` override file (gitignored, real account
names/ids) taking priority. Unmatched accounts default to `cash-other`,
flagged `source: "default"` so `./actual accounts classify` can surface them
as "needs review" rather than guessing silently. Verified live against the
user's real budget: all 8 real retirement/investment/debt accounts
classified correctly by heuristic alone; the 3 everyday checking/savings/
credit-card accounts correctly fell to "needs review" (not wrong, just
unclassified cash).

**Account balance**: the API has no running-balance field. Computed instead
via the accounting identity `sumTransactionAmounts` — the sum of an
account's entire transaction history since 1970-01-01 *is* its current
balance. Deliberately does NOT flatten splits or exclude transfers (unlike
`fetchAllTransactionsSince`, which does both for other purposes) — a split
parent's amount already equals its children's sum, and a transfer's two
legs are both real postings to two different accounts that must count;
applying either filter here would break the identity, not improve it.
Verified live: a real 401k came back at $543,147.48, and a later
`report fire` run's summed portfolio total ($2,912,155.57 across 7 accounts)
matched the sum of those same accounts' individually-reported balances
exactly.

**Status**: Phases 1-2 complete and verified live (read-only `report
accounts`, dry-run and real `report fire` writing a valid JSON file, gitignored,
never touching anything inside Actual itself). Phase 3 (a `monte-carlo-card`
widget using the `taxTreatment`/`accessAge` fields Phase 1 already computes
but Phase 2 doesn't use yet) is scoped in the plan file but deliberately left
with open sub-questions (allocation preset defaults, tax model, where
`currentAge` comes from) to resolve with the user when that phase starts —
not guessed now.

**How to apply**: the full plan (with the widget schema details, exact
`crossover-card`/`net-worth-card`/`spending-card` field shapes, and Phase 3
scoping) is at `~/.claude/plans/take-a-look-at-ethereal-dawn.md`. New files:
`src/fire-accounts.ts` (+ `.test.ts`), `src/fire-dashboard.ts` (+ `.test.ts`,
the vendored `ExportImportDashboard` types — re-check against upstream
before extending, Actual doesn't version this file independently of its own
releases), `src/accounts-classify.ts`, `src/report-fire.ts`,
`fire-accounts.example.json`. `actual-helpers.ts` gained
`fetchAllOpenAccounts`, `sumTransactionAmounts`, `fetchAccountBalance`.

**Renamed 2026-09-05, same session**: `./actual report accounts` moved to `./actual accounts classify`, its own top-level `accounts` command group (`accountsUsage`/`commandAccounts` in `actual`), not under `report` anymore. `src/report-accounts.ts` renamed to `src/accounts-classify.ts`; `package.json`'s bin entry renamed `report-accounts` -> `accounts-classify`. `report` now has just `fire`.

**Second round of renames + `classify` made interactive, same session, later that day**:
- `fire-accounts.json` -> `accounts.json` (config file name only; the module stays `src/fire-accounts.ts`, types stay `FireAccountsConfig`/`FireAccountOverride`). `DEFAULT_FIRE_ACCOUNTS_CONFIG_PATH` renamed `DEFAULT_ACCOUNTS_CONFIG_PATH`. `fire-accounts.example.json` deleted (no example file anymore).
- `./actual report fire` -> `./actual reports fire` (group `report` -> `reports`; `reportUsage`/`commandReport` -> `reportsUsage`/`commandReports`). `src/report-fire.ts` -> `src/reports-fire.ts`; bin entry `report-fire` -> `reports-fire`.
- `./actual reports fire` now **errors out** (not just warns) when `accounts.json` doesn't exist, instructing the user to run `./actual accounts classify` first — `loadClassifiedAccounts`'s `configFound: false` used to just print a warning and continue with heuristic-only classification; now it's a hard `throw`.
- **`./actual accounts classify` is now fully interactive**, not a read-only listing. For every open account it prompts a numbered choice among the 6 `FireAccountCategory` values (`promptChoice`/`openTtyInterface`, new in `actual-helpers.ts`, refactored out of `confirmViaTty`'s TTY-opening logic so one `/dev/tty` session is reused across every account instead of reopened per-question). Default precedence, per the user's explicit spec: existing `accounts.json` entry for that account, else the name heuristic, else **no default at all** — pressing Enter with no default re-prompts, there is no way to skip an unclassifiable account. `taxTreatment`/`accessAge` are derived automatically from the chosen category via a new `CATEGORY_TRAITS`/`traitsForCategory` lookup in `fire-accounts.ts` (the heuristic rules were refactored to source from the same table, removing the old per-rule trait duplication) — the interactive flow asks only one question per account (category), not three. Every run rewrites `accounts.json` from scratch with one entry per currently-open account (`writeFireAccountsConfig`, new), keyed by account **id** (not name) for robustness against renames.
- Verified live end-to-end via a pty-driven session against the real budget: all 8 real retirement/investment/debt accounts showed the correct heuristic default (bracketed `[N]`), the 3 everyday cash accounts showed no default and required an explicit choice, the written `accounts.json` was byte-correct, `reports fire --dry-run` then succeeded on top of it, and re-running `classify` afterward showed the just-written entries as the new defaults (including for the previously-unclassifiable accounts).
- `promptChoice` (unlike `confirmViaTty`) operates on the abstract `TtyInterface` rather than opening `/dev/tty` itself, so it's genuinely unit-testable with a fake `TtyInterface` double — added real tests for it in `actual-helpers.test.ts`, first real test coverage for any of this session's TTY-prompting code.

**Third round: category rename + split, same session**. `taxable-investment` -> `investment-taxable`; the combined `cash-other` fallback split into two real categories, `cash` and `other` (7 categories total now). The safe default for an unclassifiable account is `other`, not `cash` — `cash` is only ever chosen explicitly (override or interactive answer), since a name the heuristic can't recognize might not actually be cash.

**Real bug found and fixed while doing this rename, worth remembering for any future category rename**: `loadFireAccountsConfig` previously validated only `{version, accounts}` shape, not each override's `category`/`taxTreatment` value. A pre-rename `accounts.json` still on disk (e.g. one written by an earlier session before this rename) has entries like `category: "cash-other"`, which is no longer a member of `FIRE_ACCOUNT_CATEGORIES`. `FIRE_ACCOUNT_CATEGORIES.indexOf("cash-other")` returns `-1`; `accounts-classify.ts`'s old logic treated any non-null category as a valid default index, so `-1` got used as-is, displayed as a nonsensical `[0]` bracket, and if accepted (Enter), `FIRE_ACCOUNT_CATEGORIES[-1]` is `undefined` -- silently writing an entry with **no `category` field at all** (JSON.stringify drops `undefined`-valued keys) into the freshly-rewritten `accounts.json`. Caught this live, not in a test, while re-verifying the interactive flow after the rename with a leftover `accounts.json` from earlier the same session.

Fixed at the right layer: `loadFireAccountsConfig` now validates every override's `category` against `FIRE_ACCOUNT_CATEGORIES` and `taxTreatment` (when present) against a new `TAX_TREATMENTS` array, throwing a clear "unknown category/taxTreatment" error naming the offending `match` -- consistent with the function's existing "fail loudly on malformed config" doc comment, and protects every consumer (`accounts-classify.ts` and `reports-fire.ts` both), not just a defensive patch in one call site. Verified live: a stale `cash-other` entry now errors clearly instead of corrupting the rewritten file; a clean re-run of `classify` against fresh state produces a fully valid `accounts.json` with every entry's `category` a real `FIRE_ACCOUNT_CATEGORIES` member.

**Lesson for next time a category enum changes**: always test the interactive/config-loading path against a config file written by the *previous* version, not just fresh state -- a rename that's airtight against new data can still silently corrupt old data on read.

**Phase 3 (Monte Carlo) implemented, same session, later still**. `./actual reports fire -m --current-age N --target-age N` adds a `monte-carlo-card` widget. All open questions resolved with the user rather than guessed:

- **Allocation preset lives in `accounts classify`'s existing interactive flow**, not a separate interactive step in `reports-fire.ts`. After picking a category, portfolio accounts (retirement/HSA/investment-taxable) get one more numbered question -- a stock/bond mix -- with the same override-then-category-default precedence as the category question itself. This was a real design fork the user raised mid-session (should `reports fire` be interactive too?) and the answer was no: Actual's own Monte Carlo UI is already rich (return model, four withdrawal-rule types, spending phases, contributions, tax model, all with good defaults), so duplicating that as CLI prompts wasn't worth it -- our tool's unique value is the account-level facts Actual can't infer (category, tax treatment, access age, and now allocation), all collected in one place.
- **Allocation preset defaults**: `equity-80` (6.5% mean / 12% stdDev) for retirement-tax-deferred/retirement-roth/investment-taxable, `equity-60` (6%/10%) for hsa. debt/cash/other always get `allocationPreset: null` and are never asked.
- **Flat withdrawal tax rates**: 22% tax-deferred, 15% taxable, 0% tax-free/none. `taxModel: "flat"` explicitly set (matches Actual's own default, set anyway for an explicit, self-documenting generated file).
- **currentAge/targetAge are CLI flags** (`--current-age`/`--target-age` on `reports-fire.ts`), not a new `accounts.json` field -- required together whenever `-m` is given, validated `targetAge > currentAge`.
- **Scope for the rest of the widget**: pots + withdrawalStrategy ("proportional", not asked, low-stakes) + ages + one spendingPhase from the same real trailing-12-month spend already used for the crossover widget + a default `inflationMean: 0.03`. Everything else (returnModel, withdrawalRule, contributions, minimumWithdrawal, inflationStdDev, simulationCount) deliberately left unset so Actual's own UI defaults apply once the widget is opened.

**Two things found by reading Actual's real Monte Carlo source (`monteCarloSimulation.ts`) that would have produced a silently-wrong simulation if missed**:
- **`allocationPreset` alone is not enough.** It's a UI convenience only -- the simulation reads `expectedReturnMean`/`returnStdDev` directly. The vendored `ALLOCATION_PRESET_RETURNS` table in `fire-dashboard.ts` (exact numbers, copied verbatim from Actual's own `ALLOCATION_PRESETS` const) is used to set both fields explicitly on every generated pot alongside the preset label -- setting only the label would have silently fallen back to some other default at simulation time.
- **Monte Carlo Analysis is an experimental Actual feature**, off by default (Settings → Advanced → Experimental features). Both the CLI's own console output (when `-m` is used) and the README say so explicitly, since an imported widget won't render at all until the user turns this on in their own instance.

**New exports**: `fire-accounts.ts` gained `MonteCarloAllocationPreset`/`MONTE_CARLO_ALLOCATION_PRESETS`, `isPortfolioCategory`/`portfolioAccounts` (moved here from `fire-dashboard.ts`, since "which categories count as portfolio" is fundamentally an account-classification concept, not a widget-building one -- `fire-dashboard.ts`'s `portfolioAccountIds` now just wraps it), and `allocationPreset` on `FireAccountTraits`/`FireAccountOverride`/`CATEGORY_TRAITS`. `loadFireAccountsConfig`'s validation extended to also reject an unrecognized `allocationPreset`, same "fail loudly" pattern as category/taxTreatment. `fire-dashboard.ts` gained the vendored Monte Carlo types, `buildPot` (requires a non-null `allocationPreset` at the type level -- callers must filter/validate first, which `buildMonteCarloWidget` does, throwing a clear error naming the account if a portfolio account somehow has none), `buildSpendingPhase`, `buildMonteCarloWidget`.

Verified live end-to-end via a pty-driven `accounts classify` session (confirmed the allocation question only appears for portfolio categories, with correct per-category defaults) followed by `reports fire -m --current-age 45 --target-age 90`: 7 pots generated, each correctly linked by `accountId` (no hardcoded balance), correct return/stdDev per preset, correct tax rate per treatment, spending phase's `annualWithdrawal` matching the console's own printed trailing-spend figure exactly ($276,710.64), both dry-run and a real file write. 164 tests total (14 new for this phase).

**Fourth round, same session: fixed a real "existing accounts.json isn't fully respected" bug + added preset labels.** User reported two issues after using `classify` day-to-day:
- **Bug**: `classifyOneAccount` in `accounts-classify.ts` already consulted an existing override for the `category` and `allocationPreset` prompt defaults, but always recomputed `taxTreatment`/`accessAge` from `traitsForCategory(category)` (the plain category default) unconditionally -- any hand-customized `accessAge` (e.g. a real 401k's actual penalty-free-withdrawal age, which can differ from the generic default) was silently discarded and overwritten on every re-run of `classify`, even when the account's category hadn't changed. A related latent bug: the old `allocationPreset` default (`override?.allocationPreset ?? traits.allocationPreset`) didn't check whether the override's category matched the newly-chosen category, so a stale preset from a *previous* category could wrongly appear as the default for a newly-chosen different category.
- **Fix**: added a `carryOver` value (`override?.category === category ? override : null`) -- only when the account keeps the exact category it already had does `classifyOneAccount` carry forward the existing `taxTreatment`/`accessAge`/`allocationPreset`; changing the category resets all three to the new category's plain defaults instead, since a customization tuned for the old category isn't guaranteed to make sense for the new one. This is the general policy for anything derived from category the interactive flow doesn't ask about directly.
- **Labels added**: new `MONTE_CARLO_ALLOCATION_PRESET_LABELS` (`fire-accounts.ts`) maps each preset to a plain description ("100% stocks", "80% stocks / 20% bonds", etc.), displayed next to each numbered option in the allocation-preset prompt (e.g. `2) equity-80 (80% stocks / 20% bonds)`) -- the stored/returned value is still the bare preset string, only the prompt's display text changed.
- **Verified live** via a pty-driven session with a hand-crafted `accounts.json` giving one real 401k a customized `accessAge: 55` (default for its category is 59) and `allocationPreset: "equity-60"` (default for its category is `equity-80`): re-running `classify` and accepting every default showed `[3: equity-60 (60% stocks / 40% bonds)]` as the allocation prompt's default (not the category's `equity-80`), and the rewritten file preserved `accessAge: 55` untouched, confirming the carry-over gate works correctly when the category is kept the same.

**Fifth round, same session: `-m`/`--monte-carlo` removed, Monte Carlo widget always generated, ages now required.** User ran `reports fire` without `-m` (understandably, since nothing in the default invocation hinted a 4th widget existed), got a 3-widget dashboard, and reported "I don't see the monte carlo card" -- confirmed live: `fire-dashboard.json` on disk had only `net-worth-card`/`spending-card`/`crossover-card`, exactly what the flag-gated design produces without `-m`. Rather than better-documenting the flag, the user asked to remove the opt-in entirely: `reports-fire.ts`'s `-m`/`--monte-carlo` flag and its "requires ages if -m" conditional validation are gone; `--current-age`/`--target-age` are now unconditionally required (parse-time `usage()` error if either is missing), and `buildMonteCarloWidget` is unconditionally pushed onto every generated dashboard. `Options.monteCarlo: boolean` field removed; `currentAge`/`targetAge` are now plain `number` (not `number | null`) once parsed. The experimental-feature Monte Carlo console warning (enable under Settings > Advanced > Experimental features) is now printed on every real (non-dry-run) write, not just when `-m` was passed. README's `reports fire` section and its example invocations updated to match -- `./actual reports fire` alone is no longer a valid invocation; `--current-age`/`--target-age` are always required now, also noted at the account-classify allocation-question mention ("used by the Monte Carlo widget" instead of "used only if you generate the Monte Carlo widget").

**Sixth round, same session: `--current-age`/`--target-age` replaced with birth date + retirement age + an auto-computed horizon.** User flagged that `--current-age`/`--target-age` was bad UX -- current age isn't stable input (should come from a birth date, not be re-typed/re-computed by hand every run) and `--target-age` was really "estimate how long you'll live," which is a weird thing to ask a FIRE tool to make you type. Investigated by pulling Actual's real upstream `monteCarloSimulation.ts` (via `gh api repos/actualbudget/actual/contents/...`, not guessed) to find out what these fields actually drive:
- **`targetAge` has no meaning beyond `horizonYears = clamp(round(targetAge - currentAge), 1, 100)`** (`getMonteCarloHorizonYears`) -- it's pure simulation-length arithmetic, not an independently meaningful field.
- **`currentAge` is the temporal anchor every other age-based field is measured against** -- each pot's `accessAge`, each spending phase's `fromAge`. The engine's own comment: "currentAge is still needed to know when each pot's access age is reached." So it has to be a real, accurate age (from a birth date), not an arbitrary placeholder -- an accessAge of 59 (the standard tax-deferred retirement penalty-free-withdrawal age) only means the right thing if currentAge is real.
- **Non-obvious engine quirk, worth remembering for any future spendingPhases work**: with a *single* spending phase, its `fromAge` is a no-op. The per-year loop initializes `amount` from `spendingPhases[0].annualWithdrawal` *before* checking any `fromAge` condition, and only overwrites it if a later-sorted phase's `fromAge` has been reached (`plannedTodayByYear` loop in `runMonteCarloSimulation`). So a single phase's amount applies to literally every year regardless of what `fromAge` says on it -- `fromAge` only matters once there are 2+ phases.

Resolved with the user (all confirmed, no guessing): `--retirement-age` required (no default); a fixed default planning horizon of **age 100**, but with an optional `--plan-to-age N` override (not just hardcoded); pre-retirement annual withdrawal is **$0** (assumes other income covers pre-retirement spending -- the standard "still working" FIRE-calculator convention), not the same as post-retirement spend. Birth date source: `AB_BIRTH_DATE` env var (personal, stable, like `AB_BASE_URL`), overridable per-run via `--birth-date YYYY-MM-DD`; erroring clearly if neither is set.

**Implementation**:
- `actual-helpers.ts` gained `ageFromBirthDate(birthDate, asOf = new Date())` -- pure, takes an injectable `asOf` for calendar-independent tests, reuses `validateDateFormat` for the `YYYY-MM-DD` check, throws if the computed age is negative (birth date in the future).
- `fire-dashboard.ts`: `buildSpendingPhase` (singular) replaced with `buildSpendingPhases(currentAge, retirementAge, annualSpendCents)`, which returns **one** phase (`fromAge: null`, real spend) when `retirementAge <= currentAge` (already retired, or retiring today/in the past -- these collapse identically since the engine only cares about `fromAge <= age`, not exact equality timing), or **two** phases (`{fromAge: null, annualWithdrawal: 0}` then `{fromAge: retirementAge, annualWithdrawal: <real spend>}`) for a future retirement age -- this is the fix that makes retirement age actually change the simulation, given the single-phase no-op quirk above. `buildMonteCarloWidget`'s signature gained a `retirementAge` parameter (now `(x, y, accounts, currentAge, retirementAge, targetAge, annualSpendCents)`).
- `reports-fire.ts`: `--current-age`/`--target-age` gone. New: `--retirement-age N` (required), `--birth-date YYYY-MM-DD` (optional, overrides `AB_BIRTH_DATE`), `--plan-to-age N` (optional, default `DEFAULT_PLAN_TO_AGE = 100`). `main()` computes `currentAge` via `ageFromBirthDate`, validates `planToAge > currentAge` (same "must be greater than" pattern as the old current/target validation), and passes `(currentAge, options.retirementAge, options.planToAge)` into `buildMonteCarloWidget`.
- Tests: `actual-helpers.test.ts` gained an `ageFromBirthDate` describe block (whole-years counting, not-yet-had-birthday-this-year, malformed-date rejection, future-birth-date rejection). `fire-dashboard.test.ts`'s `buildSpendingPhase` describe became `buildSpendingPhases` (both branches tested); `buildMonteCarloWidget` tests updated for the new `retirementAge` argument plus a new case asserting a future retirement age threads through into the split phases. 170 tests total (6 new).
- Verified live against the real budget: `--retirement-age 60 --birth-date 1985-03-22` correctly produced `currentAge: 41`, `targetAge: 100` (default), and the two-phase split (`$0` until 60, then the real `$276,710.64/yr` trailing spend from 60 on); `--retirement-age 30` (in the past) correctly collapsed to the single always-on phase; `--plan-to-age 95` correctly overrode the default; `--plan-to-age 30` (below current age) correctly errored; `AB_BIRTH_DATE` env var and its `--birth-date` override both resolved to the expected `currentAge`; missing both birth-date sources errored clearly.
- README: `reports fire`'s usage line, flag docs, prose (spending-phase-split explanation), and example invocations all rewritten; `AB_BIRTH_DATE` added to the top-level config env var table with a note it's `reports-fire`-only (the table's other four vars are truly global across every command).

**Seventh round, same session: compare multiple retirement ages.** User wanted several retirement ages plotted, "preferably on a single monte carlo chart." Checked Actual's real widget model (`dashboard.ts`'s `MonteCarloWidget` type, `monteCarloSimulation.ts`) and the Monte Carlo UI directory (`MonteCarlo.tsx` etc.) for any multi-config comparison feature -- none exists. Every "scenario" in that codebase means one of the N individual simulation *runs* within a single config's percentile bands, not a distinct user-configured comparison. **A single `monte-carlo-card` widget holds exactly one config; there's no overlay/compare mode.** Told the user this constraint plainly rather than guessing around it, then implemented the closest real equivalent: one stacked widget per retirement age on the same dashboard page.

- `--retirement-age` on `reports-fire.ts` is now **repeatable** (`retirementAges: number[]`, `.push()`-accumulated), following this repo's existing repeatable-flag convention (`-c/--category` in `anomalies.ts`/`set-budget.ts`). Validation message follows the same house style: `"At least one --retirement-age is required."` (mirrors `anomalies.ts`'s `"At least one -c/--category is required."`).
- `fire-dashboard.ts`: `buildMonteCarloWidget` gained an optional trailing `name` parameter (default `"Monte Carlo"`, backward compatible with every existing call site/test). New `buildMonteCarloWidgets(x, y, accounts, currentAge, retirementAges, targetAge, annualSpendCents)` maps over the ages, stacking each widget's `y` by a new exported `MONTE_CARLO_WIDGET_HEIGHT` constant (4, matching the widget's own `height`) so they never overlap. A single age keeps the plain `"Monte Carlo"` name (no behavior change for the common case); 2+ ages get `"Monte Carlo — Retire at ${age}"` per widget so they're distinguishable on the page.
- `reports-fire.ts`'s `main()` now does `dashboard.widgets.push(...buildMonteCarloWidgets(...))` (spread, since it returns an array) instead of pushing one widget.
- Tests: `fire-dashboard.test.ts` gained a `buildMonteCarloWidgets` describe block (single-age plain name, multi-age unique names + correct non-overlapping stacked `y` coordinates + per-widget correct spending phases) plus a `buildMonteCarloWidget` name-override test. 174 tests total (4 new).
- Verified live: `--retirement-age 55 --retirement-age 60 --retirement-age 65` produced three `monte-carlo-card` widgets at `y: 6, 10, 14` named `"Monte Carlo — Retire at 55/60/65"`; a single `--retirement-age 60` still produces one widget named plain `"Monte Carlo"` (no regression for the common case); omitting `--retirement-age` entirely still errors clearly.
- README: `--retirement-age` flag description, a new "Comparing multiple retirement ages" callout explaining the stacked-not-overlaid reality, and a three-age example invocation added.

**Eighth round, same session: short flags added.** `-r`/`--retirement-age`, `-b`/`--birth-date`, `-p`/`--plan-to-age` (none conflict with this file's existing `-o`/`-f`/`-n`/`-h`). Usage line shortened to `-r N` in the summary. README examples switched to the short forms (`-r 55 -r 60 -r 65`).

**Ninth round, same session: dropped the spending widget, net worth spans full width, and regeneration now preserves local customizations to the output file.** Two independent requests:

1. **Layout**: `buildSpendingWidget`/`SpendingCardMeta` removed entirely (dead code, not just unused from `buildFireDashboard`); `spending-card` dropped from `FireWidgetType`. `buildNetWorthWidget` now emits `width: 12` (was 6, sharing the row with spending). `buildFireDashboard` now assembles just `[net-worth-card, crossover-card]` -- no layout-coordinate changes needed elsewhere since net worth was already the same height (2) as the old spending card, so crossover's `y: 2` and the Monte Carlo widgets' `y: 6` start were already correct.

2. **Preserve customizations on regeneration**: previously every `reports fire` run fully overwrote the output file, silently discarding any post-import tweak (in Actual's own Monte Carlo config UI, copied back into the file) or direct hand-edit. New `mergeGeneratedDashboard(generated, existing)` in `fire-dashboard.ts` merges a freshly built dashboard with whatever's already at the output path, and is now unconditionally applied in `reports-fire.ts`'s `main()` (`loadExistingDashboard` reads+`JSON.parse`s the existing file if present, tolerates a missing/malformed file by warning and falling back to a fresh generation, never crashes the run over a bad existing file) -- **no flag**, this is just the new default behavior.
   - **Confirmed with the user before implementing** (two real forks, no guessing): a Monte Carlo widget for a retirement age no longer passed via `-r` is **deleted**, not kept around forever; a widget of a type this tool has never generated (hand-added, e.g. a note card) is **kept completely untouched**.
   - **Widget identity for matching**: `net-worth-card`/`crossover-card` are singletons (matched by type alone); `monte-carlo-card` is matched by `meta.name`, which already uniquely encodes the retirement age (`buildMonteCarloWidgets`'s naming) -- so an age dropped from the CLI args has no generated counterpart to match against and is naturally excluded, no special-case code needed for that half of the "drop" behavior.
   - **Field-level ownership within a matched widget**: always-refreshed ("real data, this run's inputs") vs. preserved-if-present ("assumption/config, yours to tune"). `net-worth-card` owns nothing -- an existing customization wins outright. `crossover-card` owns only `expenseCategoryIds`/`incomeAccountIds`; `safeWithdrawalRate`/`estimatedReturn`/`projectionType`/etc. are preserved if customized. `monte-carlo-card` owns `pots` (merged sub-field-by-sub-field, matched by pot `id` = account id: `{...existingPot, ...generatedPot}` so an extra field like `annualFeeRate` survives while every core field -- `allocationPreset`, `expectedReturnMean`, `accessAge`, `withdrawalTaxRate` -- always refreshes from current `accounts.json`), `spendingPhases` (owned ids `"pre-retirement"`/`"retirement-spending"` always fully regenerated -- a stale one whose future retirement age was since removed is dropped, not carried forward -- any other phase id, e.g. a hand-added `"downsize"` phase, is untouched and appended), `currentAge`, `targetAge`, and `name`. Everything else on `monte-carlo-card` (`withdrawalStrategy`, `inflationMean`, `taxModel`, and anything this tool has never set at all -- `returnModel`, `withdrawalRule`, `contributions`, `minimumWithdrawal`, `inflationStdDev`, `simulationCount`, `taxBands`) is preserved.
   - Layout (`x`/`y`/`width`/`height`) always comes from the fresh generation on every matched widget -- it's a function of how many widgets this run produces (see the net-worth-width and stacking changes above), not something meaningful to hand-tune in the file.
   - New types: `ExistingDashboardWidget`/`ExistingDashboard` in `fire-dashboard.ts` -- deliberately loose (`type: string`, not the narrower `FireWidgetType`) since a file read back from disk isn't guaranteed to match this module's current schema (an older or hand-edited file, or a type this version no longer generates like the just-removed `spending-card`).
   - Tests: a new `mergeGeneratedDashboard` describe block in `fire-dashboard.test.ts` (no-existing-file passthrough, net-worth full preservation, crossover id-refresh + assumption-preservation, pot core-field-refresh + extra-field-preservation, spending-phase owned-refresh + extra-phase-preservation, age-dropped widget deletion, foreign-widget passthrough). 180 tests total (7 new).
   - Verified live against the real budget: hand-customized the real `fire-dashboard.json` (`safeWithdrawalRate: 0.035` on crossover, `returnModel: "historical-sample"` + `annualFeeRate: 0.001` on every Monte Carlo pot, ages 53/55/59), re-ran `reports fire -r 53 -r 55 -r 59` and confirmed all three customizations survived exactly; then re-ran with `-r 55 -r 59` only (dropping 53) and confirmed the 53 widget was deleted while 55/59 kept their customizations. Original file restored afterward.
   - README gained a "Regenerating preserves customizations..." section; `reports fire`'s top-level description also updated to mention this instead of the now-removed spending widget.

**Tenth round, same session: `./actual configure` replaces `accounts classify` -- a large feature covering ALL crossover/Monte Carlo configuration, not just account classification.** User asked for one command that "does all the configuration," with `config.json` replacing `accounts.json`, two-way merge between `config.json` and `fire-dashboard.json`, and a deferred `--UI` web mode. Given the size, went through `EnterPlanMode` first rather than diving in -- research findings and decisions from that phase (all confirmed via `AskUserQuestion`, not guessed):

- **`@actual-app/components` (Actual's own React design-system package, used by every Monte Carlo/crossover config screen) is confirmed NOT published to npm** (`npm view` 404) -- it's a private, monorepo-internal package tightly coupled to React + Emotion + react-aria-components + i18next + Actual's own `#components/...` path aliases. Literal component reuse for a future `--UI` isn't a small lift. **User's call: vendor a subset anyway when that phase happens** (not "mirror the look" as originally recommended) -- Phase 2, explicitly not scoped or built this round.
- **Delivery is phased**: this round is CLI-only (`configure` + schema + two-way merge + `reports-fire.ts` made config-driven); `--UI` is a separate future phase.
- **Contributions unification**: crossover's `expectedContribution` and Monte Carlo's per-pot `contributions` don't have a natural 1:1 shape (one is a flat total, the other is per-account with its own age window) -- resolved by asking a monthly contribution **per portfolio account** (same accounts that already get the allocation question), summing them into crossover's flat number, and feeding each account's own amount x12 into its own Monte Carlo contribution entry. **Verified against Actual's real `Crossover.tsx` source** (not the ambiguous upstream doc comment, which literally says "monthly dollar amount" but actually means minor units/cents like everything else in this schema -- confirmed by the UI dividing the same field by 100 before display) before implementing, avoiding what would have been a ~100x unit bug.
- **`birthDate` moved into `config.json`** as the primary source; `AB_BIRTH_DATE`/`--birth-date` remain overrides.

**Exact field inventory** (re-verified against upstream `dashboard.ts`'s real enum definitions during planning, not recalled from memory -- these differ from earlier guesses): `MonteCarloReturnModel = 'normal' | 'historical-bootstrap' | 'historical-sequence'`; `MonteCarloWithdrawalRuleType = 'none' | 'guardrails' | 'ratcheting' | 'floor-ceiling' | 'boundaries'` with ~13 optional sub-fields grouped by type; `CrossoverWidget` also has a `showHiddenCategories` boolean and a `timeFrame` (left un-asked, advanced/rare, only preserved via existing merge if hand-set).

**Schema** (`src/fire-accounts.ts`, mechanical rename `FireAccountsConfig`->`FireConfig`, `loadFireAccountsConfig`->`loadFireConfig`, `writeFireAccountsConfig`->`writeFireConfig`, `DEFAULT_ACCOUNTS_CONFIG_PATH`->`DEFAULT_CONFIG_PATH = "config.json"`, no back-compat aliases): `FireConfig` gained `birthDate: string | null`, `retirementAges: number[]`, `planToAge: number`, `crossover: CrossoverAssumptions`, `monteCarlo: MonteCarloAssumptions`; `FireAccountOverride`/`ClassifiedAccount` gained `monthlyContribution` (cents). `loadFireConfig` now **always returns a fully-populated `FireConfig`**, backfilling missing top-level sections with documented defaults (`DEFAULT_CROSSOVER_CONFIG`, `DEFAULT_MONTE_CARLO_CONFIG`, both exported) rather than erroring -- an old `accounts.json`-shaped file loads and migrates forward cleanly (verified live: the real pre-existing `accounts.json` loaded with `found: true`, all 11 real accounts intact, new sections defaulted). Validation extended to the new enums following the exact same "fail loudly, name the field and valid values" pattern as category/taxTreatment/allocationPreset already used.

**Avoiding a real fire-accounts.ts <-> fire-dashboard.ts import cycle**: the assumption-shaped types (`CrossoverAssumptions`, `MonteCarloAssumptions`, `MonteCarloReturnModel`, `MonteCarloWithdrawalRuleMeta`, `MonteCarloTaxBandMeta`, `MonteCarloContributionMeta`, `CrossoverProjectionType`) live in `fire-dashboard.ts` (this project's one home for vendored Actual widget shapes) and `fire-accounts.ts` imports them with `import type` for its `FireConfig` schema. Since `verbatimModuleSyntax`/`erasableSyntaxOnly` are on (tsconfig), `import type` lines are fully erased by Node's `--experimental-strip-types` at runtime -- so even though `fire-dashboard.ts` also does a real (value) `import { portfolioAccounts } from "./fire-accounts.ts"`, there's no actual runtime circular dependency, only a (harmless, TS-supported) circular *type* reference. Confirmed working via `tsc --noEmit` and real execution, not just assumed.

**`src/fire-dashboard.ts` widened**: `buildCrossoverWidget`/`buildFireDashboard`/`buildMonteCarloWidget`/`buildMonteCarloWidgets` now take the assumption objects as parameters instead of hardcoding Actual's UI defaults inline -- those defaults now live in `fire-accounts.ts`'s `DEFAULT_CROSSOVER_CONFIG`/`DEFAULT_MONTE_CARLO_CONFIG` (used by `loadFireConfig`'s backfill and `configure.ts`'s prompt defaults), not in the builder functions. New `buildContributions` (internal) builds one `MonteCarloContributionMeta` per portfolio account with a nonzero `monthlyContribution` (`annualAmount = monthlyContribution * 12`); new exported `totalMonthlyContribution` sums the same population for crossover's `expectedContribution` (returns `null`, not `0`, when nothing's configured, matching Actual's own "unset" convention).

**New: `extractConfigFromDashboard`** (`fire-dashboard.ts`) -- the reverse direction of `mergeGeneratedDashboard` (dashboard -> config instead of config -> dashboard). Reads a `crossover-card`'s assumptions and a `monte-carlo-card`'s assumptions/contributions/retirement-ages back into a `FireConfig`, defensively (structural `typeof`/`Array.isArray` checks with fallback to the existing config value, same defensiveness tier as the existing `mergePots`/`mergeSpendingPhases` -- no exact enum-membership validation at this layer, deferred to the next `loadFireConfig` read). Never touches `birthDate`/`planToAge`/account category-taxTreatment-accessAge-allocationPreset (real-account-data-owned, never dashboard-derived). Multiple Monte Carlo widgets (multi-retirement-age comparisons) all read from the *first* one for assumptions (they should agree since this tool generates them identically) but retirement ages are reconstructed from *every* one's `retirement-spending` phase.

**Real bug found by a round-trip test, not by inspection**: initially forgot to wire `simulationCount` into both `MonteCarloCardMeta` (the type) and `buildMonteCarloWidget`'s returned meta object -- a `extractConfigFromDashboard` round-trip test (`toEqual(customMonteCarlo)`) caught the mismatch immediately (`5000` instead of the test's `7500`) before this ever reached the real budget. Fixed by adding the field to both places. **Second real bug, found while writing the README, not a test**: `mergeMonteCarloMeta`'s field-ownership list never included `contributions` -- since contributions are now real per-account data (not a hand-tunable assumption), a stale contributions array in an existing dashboard file would have silently survived regeneration forever, never picking up an account's updated `monthlyContribution`. Fixed with a new `mergeContributions` (mirrors `mergeSpendingPhases`'s owned-id-refresh-plus-extra-preservation pattern, keyed by the generated `contribution-<accountId>` id), added to `mergeMonteCarloMeta`'s owned-fields list, with a dedicated regression test. **Lesson reinforced**: adding a new generated field to a widget's meta means auditing every place that widget's fields are enumerated (the type, the builder, AND the merge's ownership list) -- this is the second time in this project a newly-added field was missed in one of several parallel enumeration sites (see the third-round "category rename" lesson earlier in this file for the same shape of mistake).

**`src/configure.ts`** (new, replaces deleted `src/accounts-classify.ts`): same `TtyInterface`/one-open-session/write-after-every-step pattern as the old `accounts-classify.ts`. Question flow: per-account (category -> allocation -> NEW monthly contribution, portfolio accounts only) -> birth date -> retirement age(s) (new `askRetirementAges`, y/n "add another?" loop defaulting to yes when the existing config already had more ages than asked so far) -> plan-to-age -> crossover assumptions -> Monte Carlo assumptions (withdrawal strategy/return model/withdrawal rule -- sub-fields only asked for a non-"none" rule type/minimum withdrawal/inflation mean+stddev/tax model -- band questions only if "bands"/simulation count). Percent-style fields asked as human percents (e.g. "4" for 4%), stored /100; dollar fields asked in dollars, stored in cents. New shared helpers added to `actual-helpers.ts`: `promptNumber` (free-text numeric input, mirrors `promptChoice`'s loop-until-valid style) and `confirmOnTty` (y/n on an *already-open* `TtyInterface`, since `confirmViaTty` opens/closes its own session per call -- wrong for a many-question flow like this one).

**Dashboard-import detection**: `configure` checks at startup whether the dashboard file (default `fire-dashboard.json`, `-d`/`--dashboard` overridable) exists and is newer (mtime) than the config file; if so, offers via `confirmOnTty` to run `extractConfigFromDashboard` first, before asking anything else. Verified live end-to-end: hand-edited a real generated `fire-dashboard.json`'s `returnModel`/`simulationCount`, bumped its mtime forward, ran `configure`, confirmed the offer appeared and (once actually answered "y" -- see testing note below) correctly updated `config.json`.

**`src/reports-fire.ts`** now reads everything from `config.json` by default; `-r`/`-b`/`-p` become **optional overrides** for a single run (a given `-r` replaces the whole configured retirement-age list for that run, doesn't append). Verified live: `reports fire` with **zero** age/birth-date flags now works purely off `config.json` and reflects its configured assumptions (`safeWithdrawalRate`, `withdrawalStrategy`, `simulationCount`, etc., not old hardcoded defaults) in the generated widgets.

**Dispatcher/packaging**: `accounts` command group removed entirely from `actual` (bash) -- `classify` was its only subcommand, so the whole group (`accountsUsage`/`commandAccounts`) was deleted rather than left as an empty shell; `configure` added as a direct top-level case (same pattern as `build`/`lint`/`test`, not a subcommand group, since it has no subcommands of its own). `package.json`'s `accounts-classify` bin entry -> `configure`. **Forgot `chmod +x` on the new `src/configure.ts` file** after creating it with the Write tool (unlike `Edit`, which preserves an existing file's permissions, a fresh `Write` doesn't carry over the shebang-implies-executable convention) -- caught immediately by `./actual configure --help` failing with "Permission denied," fixed with `chmod +x`. **Lesson for next new executable CLI file**: `chmod +x` is a required step after `Write`-creating one, not automatic.

**Rendering approach decision (no template engine)**: considered whether to introduce a literal templating engine (handlebars/mustache/etc.) instead of the existing typed builder-function approach. Decided against it -- none is a current dependency, and a real template engine would work against the two-way merge (which needs to inspect/compare individual typed fields, not stamp out text). The existing one-function-per-widget-shape pattern already serves as "the template" in every practical sense; kept as-is, just parameterized with config-driven values instead of hardcoded constants.

**Testing note -- pty-driven multi-step interactive flows are timing-sensitive**: a naive pty bot that answers on every idle-select timeout (rather than waiting for output to actually stabilize) sends premature blank keystrokes during the async pre-first-prompt delay (account/balance fetching), which get consumed out-of-order by later prompts once they appear -- this produced a false negative (`configure`'s dashboard-import offer looked declined when it wasn't) before a corrected bot (require output to stop growing for >0.6-0.8s before answering) confirmed the real behavior was correct. Real config.json/fire-dashboard.json in the repo root were used for this (backed up to `/tmp` first, restored afterward with fabricated test-only personal fields like the birth date scrubbed out before leaving them in place) -- migrating the pre-existing real `accounts.json` forward and deleting it (superseded by `config.json`) was part of this round's real live verification, not just synthetic test data.

**Deliberately deferred, not part of this round**: the `--UI` web interface itself (framework/server choice, port, whether to auto-open a browser, and the actual vendoring-a-subset-of-`@actual-app/components` work) -- confirmed as its own future phase, nothing about it decided or scaffolded yet beyond the phase-1/phase-2 split itself. 204 tests total (24 new this round, plus one regression test for the contributions-merge bug found mid-round).

**Eleventh round, same session: real Actual "?" tooltip text surfaced in every `configure` prompt, plus IRS contribution limits shown for retirement/HSA accounts.** Two follow-up requests after the `configure` round above shipped:

1. **User asked what "expense adjustment factor" (crossover) means.** Answered by reading the real `crossover-spreadsheet.ts` computation (not the one-line vendored doc comment): it's a plain multiplier applied to the projected expense figure right before the crossover check (`adjustedExpenses = projectedExpenses * adjustmentFactor`; the crossover fires once projected income >= *adjusted* expenses, not raw). Then, on request, pulled the **real tooltip text Actual's own UI shows for this and every other crossover/Monte Carlo config field**, straight from `Crossover.tsx`/`MonteCarloConfiguration.tsx`/`MonteCarloWithdrawalRuleConfiguration.tsx`/`MonteCarloTaxConfiguration.tsx` (via `gh api .../contents/<path> -H "Accept: application/vnd.github.raw"`, not guessed) -- these are meaningfully better than what either of us had written from memory. Notable finding: Actual's own UI doesn't call this field "expense adjustment factor" to the user at all -- it's labeled **"Target Income (% of expenses)"**, with 100/above-100/below-100 framed as "income equal to expenses / plan to spend more / plan to spend less." `configure.ts`'s prompt now matches that exact framing and asks it as a percent (e.g. "110" for +10%), not a raw multiplier.
2. **Added a new `explain()` helper in `configure.ts`** (prints a line of context via `process.stderr.write` immediately before a question, the CLI equivalent of Actual's "?" tooltip icon) and wired the real, lightly-condensed tooltip text into every crossover and Monte Carlo prompt, including a short explanatory line for each of the four withdrawal-rule types (guardrails/ratcheting/floor-ceiling/boundaries) adapted from Actual's own fill-in-the-blank sentence UI for those.
3. **User asked to also surface current IRS contribution limits next to the retirement/HSA monthly-contribution question.** Verified exact current-year figures via a real web search plus a direct `WebFetch` of the IRS's own newsroom page (not recalled from memory or trusted from a single secondary source, several of which quoted the catch-up amounts ambiguously) — confirmed 2026 figures: 401(k)/403(b)/457/TSP $24,500 standard ($32,500 if 50-59-or-64+, $35,750 if 60-63 under SECURE 2.0's "super catch-up"); Traditional/Roth IRA $7,500 combined limit ($8,600 if 50+); HSA $4,400 self-only / $8,750 family (+$1,000 if 55+ per spouse, per IRS Revenue Procedure 2025-19).
4. **User explicitly wanted these pulled "dynamically" rather than hardcoded** -- investigated and reported honestly that there's no real IRS API (only annual HTML news releases + Revenue Procedure PDFs) and that a runtime scrape (of either irs.gov or, per the user's specific next suggestion, DuckDuckGo) would be fragile and a new kind of dependency for a tool that has never made an outbound request to anything but Actual's own API -- also flagged the concrete technical fact that DuckDuckGo has no general search API a plain script can call (their Instant Answer API isn't a web search API and wouldn't reliably answer this kind of query), so "dynamic" would in practice mean scraping DDG's HTML results page, against their terms and less authoritative than the primary source. **Resolved with the user**: a small git-committed local data file (`irs-limits.json`, NOT gitignored -- it's generic reference data, not personal, unlike `config.json`) that a future session updates on request via a real web lookup, not a runtime fetch.
5. **New `src/irs-limits.ts`** (+ `.test.ts`): `IrsLimits` type, `loadIrsLimits(path)` (never throws -- missing/malformed returns `null`, since this is advisory prompt context, never required data), `isIrsLimitsStale(limits, asOf)` (compares `asOf`'s calendar year against the file's `taxYear`). `configure.ts` gained `-i`/`--irs-limits PATH` (default `irs-limits.json`) and a new `explainIrsLimits(category, limits)` that prints the relevant limit(s) right before the monthly-contribution question for `retirement-tax-deferred`/`retirement-roth`/`hsa` only (no limit exists for `investment-taxable`). Since the heuristic/category scheme can't distinguish a 401(k)-type account from a traditional IRA within `retirement-tax-deferred` (same for Roth 401(k) vs Roth IRA under `retirement-roth`), **both relevant limits are shown together** so the person picks the one that actually applies to that specific account -- deliberately not guessed from the account name past what the existing heuristic already does. A stale file (calendar year past `taxYear`) appends a "-- these are `<year>` figures and may be out of date" warning to the explanation line rather than presenting it as current.
6. Verified live against the real budget/real `config.json`: the reference lines rendered correctly and with the exact right dollar figures for all 11 real accounts (4x `retirement-tax-deferred`, 1x `retirement-roth`, 1x `hsa`, others unaffected), and the real `config.json`'s 11 accounts/birth date/retirement ages/assumptions all survived a full re-run unchanged (confirming defaults still carry over correctly with the new explanatory lines layered on top, no behavior regression). 210 tests total (6 new, all in `irs-limits.test.ts`).
7. README gained an "IRS contribution limits" section (mirrors the "config.json" section's style) plus a note in the account-classification walkthrough and the new `-i` flag in the options list and usage line.

**Twelfth round, same session: `-s`/`--section` lets a re-run skip most of the questions.** User wanted a quick way to update just one thing (e.g. this year's contributions) without re-answering all ~25 questions every time. New repeatable `-s`/`--section` flag narrows a run to just the named group(s): `accounts`, `personal` (birth date + retirement ages + plan-to-age, bundled since they're three quick prompts), `crossover`, `monte-carlo`. Default (no `-s` given) is unchanged -- all four, same as before this round.
- `main()` restructured so each of the four groups' `ask*` call (and, for `accounts`, the `fetchAllOpenAccounts`/`fetchAccountBalance` calls themselves, not just the prompts -- a real efficiency win, not just fewer questions) is gated on `sections.includes(...)`; a skipped section's field(s) fall through to `existingConfig`'s current value unchanged, via `let` bindings initialized from `existingConfig` before the loop/branches.
- **Safety net**: on a first run (no existing config file found), `accounts` is force-included even if not named via `-s` -- skipping it entirely on a fresh setup would leave `reports fire` with an empty account list and no portfolio to work from. A clear console line explains this when it happens, and a separate line lists which sections are actually running whenever fewer than all four are selected.
- Unknown section names are rejected with the same `usage()`-and-exit pattern as every other validated flag in this repo, naming the valid values.
- Verified live against the real budget/config: `./actual configure -s crossover` asked only the 5 crossover questions (confirmed via a pty-driven transcript) and, after accepting every default, left `accounts`/`birthDate`/`retirementAges`/`monteCarlo` byte-identical to a pre-run backup while `crossover` itself round-tripped unchanged; `./actual configure -s bogus` errored clearly; deleting `config.json` and running `-s crossover` correctly force-included `accounts` with the explanatory note. 210 tests still pass unchanged (this was a pure CLI-flow/wiring change in `configure.ts`, no new pure logic in `fire-accounts.ts`/`fire-dashboard.ts` to unit test).
- README's `./actual configure` section gained the `-s` flag, its four valid values, and the "quick re-run" use case in its usage/options block.

**Thirteenth round, same session: per-choice descriptions inline, and `%` on percent defaults.** Two focused formatting requests:
1. **Per-choice descriptions moved next to each choice**, matching the pattern already used for the allocation-preset prompt (e.g. `equity-80 (80% stocks / 20% bonds)`), instead of one long paragraph before the prompt trying to explain every option at once. New label maps in `fire-accounts.ts` next to each enum's array (`CROSSOVER_PROJECTION_TYPE_LABELS`, `MONTE_CARLO_WITHDRAWAL_STRATEGY_LABELS`, `MONTE_CARLO_RETURN_MODEL_LABELS`, `MONTE_CARLO_WITHDRAWAL_RULE_TYPE_LABELS`, `MONTE_CARLO_TAX_MODEL_LABELS`), condensed from the same real Actual tooltip text pulled last round. New `labeledOptions(values, labels)` helper in `configure.ts` builds the `"value (description)"` display array; the bare `values` array is still passed as `promptChoice`'s `defaultLabels` param so the bracket shows just the plain value (e.g. `[2: sequential]`, not `[2: sequential (drain the first pot...)]`). The preceding `explain()` paragraphs were trimmed down to one general-framing sentence each, since per-choice detail now lives inline.
2. **`promptNumber` gained an optional `unit` parameter** (default `""`) appended only to the displayed default bracket (`[4%]`), never to typed/accepted/returned values -- every percent-style question in `configure.ts` (safe withdrawal rate, estimated return, target income, all four withdrawal-rule types' percentage sub-fields, tax band rate, inflation mean/stddev) now passes `"%"`. Added a `promptNumber` describe block to `actual-helpers.test.ts` (this function had shipped two rounds ago with zero test coverage despite memory notes claiming otherwise -- gap closed now, 7 new tests including the unit-only-affects-display-not-parsing case).
3. **Real config-integrity incident, caught and fixed, not swept under the rug**: while live-verifying this round's formatting with a pty bot chaining many prompts together (`-s crossover -s monte-carlo`), noticed the real `config.json`'s `monteCarlo` section had silently drifted across two different sets of non-default values between successive rounds' automated tests (traced to blank-driven pty bot timing artifacts over long multi-prompt chains, not a `configure` code bug -- confirmed by a completely clean, byte-identical round trip using the same bot on the shorter, single-section `-s crossover` alone, both before and after this round's changes). Disclosed to the user immediately, checked for orphaned processes (none found), confirmed `crossover`/`accounts`/`birthDate`/`retirementAges` were untouched, and reset `monteCarlo` to `DEFAULT_MONTE_CARLO_CONFIG`'s plain system defaults rather than leave test noise in real financial-planning data. **Lesson for future live verification of `configure`**: prefer short, single-section pty runs (`-s <one thing>`) over long chained multi-section ones for anything touching the real `config.json`, and snapshot-and-restore (`cp` before, diff after, restore) rather than trusting the run left things exactly as intended.
4. 217 tests total (7 new, all in the new `promptNumber` describe block -- the formatting/label changes themselves are display-only and covered by the existing live-verification pattern, not additional unit assertions, since `labeledOptions` has no interesting behavior beyond string concatenation already implicitly exercised by every `promptChoice` call site).

**Fourteenth round, same session: drop redundant prompt text now that explain() covers it, and save after every individual question (not just every section).** Two follow-ups in immediate succession:

1. **Redundant prompt text removed.** User pointed out the output looked like `"Safe withdrawal rate: ...\nSafe withdrawal rate, as a percent (e.g. 4 for 4%) [4%]: "` -- the explain() line already named and explained the field, so the prompt's own text just repeated it. Fixed at the root: `promptChoice`/`promptNumber` now `.trimStart()` their constructed question line, so an empty `promptText` (used everywhere a preceding `explain()` already fully covers the question) cleanly collapses to just the bracketed default (e.g. `[4%]: `) with no stray leading space. Went through every question in `configure.ts` and emptied `promptText` wherever it was purely redundant with an immediately-preceding `explain()` describing that *same single field* -- kept real labels where multiple sub-questions follow one `explain()` block and need to stay distinguishable (all four withdrawal-rule types' multi-field groups, tax bands' per-band threshold/rate, per-account category/allocation/contribution, which have no preceding explain() at all).
2. **`%` on percent-prompt defaults.** `promptNumber` gained an optional `unit` param (default `""`), appended to the displayed default only (`[4%]`, never affecting typed/parsed/returned values); every percent-style question in `configure.ts` now passes `"%"`. Added the `promptNumber` test coverage this function had been missing since it shipped two rounds ago (7 new tests, including one asserting the unit only affects the *display*, not accepted input or the return value).
3. **Real config-integrity incident #2, caught mid-verification, disclosed and fixed, not hidden**: while live-testing the label formatting with a long chained `-s crossover -s monte-carlo` pty run, found the real `config.json`'s `monteCarlo` section had drifted a second time across rounds to yet another set of non-default values nobody chose -- traced to the same class of pty-bot timing artifact over long prompt chains identified in round 13 (not a `configure` bug: a short, single-section `-s crossover` run round-tripped byte-identical both before and after, proving the underlying code path is correct). Checked for orphaned processes (none), confirmed only `monteCarlo` was affected, reset it to `DEFAULT_MONTE_CARLO_CONFIG`'s plain defaults again.
4. **"Update after every question, not just every section" (the user's literal ask, and now literally true)**: previously `configure.ts` wrote the file once per *section* (once per account, but only once total for all of crossover's 5 questions, all of Monte Carlo's ~10+, and once for the whole withdrawal-rule sub-flow or all of a tax-bands loop) -- a crash mid-section could lose several already-answered questions. Restructured around a mutable `draft: FireConfig` built once in `main()` (nested `crossover`/`monteCarlo`/`monteCarlo.withdrawalRule`/`monteCarlo.taxBands` explicitly cloned from `existingConfig` so mutating them can never reach back into the loaded object) plus a `Save = () => void` closure (`writeFireConfig(path, draft)`) threaded into every `ask*` function. Every one of those functions now mutates one field of `draft` at a time and calls `save()` immediately after, instead of collecting local answers and returning one aggregate object at the very end. `classifyOneAccount` similarly writes into `draft.accounts[index]` (pre-existing entries at other indices untouched) after each of its up-to-three sub-questions, not just once per account.
   - **Verified live with a precision test, not just a full round-trip**: ran `-s crossover`, answered *only* the first question (typed `5` for safe withdrawal rate, a value distinct from the existing `4`), then `kill()`ed the process immediately afterward, before the second question could even be asked -- confirmed via `ps`/reading `config.json` mid-flight that `safeWithdrawalRate` had already updated to the new value on disk while `estimatedReturn`/`projectionType`/`expenseAdjustmentFactor`/`showHiddenCategories` were still untouched. This is the strongest possible proof of "after every question, not every section" short of formal instrumentation.
   - **Lesson reinforced from round 13, applied properly this time**: every real-config live-verification in this round used the snapshot-before/diff-after/restore-after pattern religiously (`cp config.json ... ; ... ; diff/compare ; cp back`), including for the destructive precision kill-test above -- no further drift into the user's real file this round.
5. README's `config.json` section wording updated from "every step writes... not just at the very end" to "after every single question... never loses more than the one answer in flight," matching the new, stronger guarantee.

**Fifteenth round, same session: readable multi-line explanations, reordered guardrails/floor-ceiling text, a missing explain() filled in, opaque "fetch failed" fixed, and a testing-methodology change.**

1. **`explain()` now takes multiple string arguments, joined by `\n`**, instead of one long concatenated sentence -- a wall-of-text paragraph (e.g. the "Target income" explanation, 4 sentences run together) is genuinely hard to parse in a terminal. Every multi-sentence `explain()` call in `configure.ts` was split into one argument per sentence/clause.
2. **Two real order-mismatch bugs found and fixed while splitting the guardrails/floor-ceiling text**: the guardrails explanation described "Capital preservation" (rises→cut) before "Prosperity" (falls→raise), but the actual questions ask Prosperity's two fields first, then Preservation's -- reversed order made the explanation and the questions hard to correlate. Reordered the *explanation* (not the underlying `MonteCarloWithdrawalRuleMeta` field order, which mirrors upstream) to Prosperity-then-Preservation, matching question order. Same issue in floor-ceiling (explained "ceiling...floor" but asks Floor then Ceiling) -- reordered to "floor...ceiling." Boundaries was already correctly ordered (upper-then-lower matches both explanation and questions), left as-is content-wise, just line-split.
3. **`showHiddenCategories` was the one crossover field with no preceding `explain()` at all** -- user read this as "doesn't have a default," though the actual default mechanism (`confirmOnTty`'s `[y/N]`/`[Y/n]`) was already working correctly (verified live by forcing the stored value to `true` and confirming the prompt showed `[Y/n]`). Added a short explain() line (no real Actual tooltip exists for this minor selector-display toggle, so this one is our own plain wording, not adapted copy) rather than a code fix, since there was no actual default bug.
4. **New shared `formatError(error)` in `actual-helpers.ts`**, used by every CLI's top-level `main().catch(...)` (and a few inline error-message interpolations) across the whole repo (`reports-fire.ts`, `configure.ts`, `anomalies.ts`, `set-budget.ts`, `match-uncleared.ts`) -- appends `error.cause`'s message (plus its `.code` when present, e.g. `ECONNREFUSED`/`ENOTFOUND`) when present. Motivated by the user hitting a bare, useless `fetch failed` from `./actual reports fire` on their own machine -- Node's native `fetch()` throws exactly that generic `TypeError` message on any network-level failure, with the actually-useful detail (connection refused, DNS failure, TLS error, ...) attached only as `.cause`, which plain `error.message` silently drops. Verified live by pointing `AB_BASE_URL` at an unreachable port/host and a nonexistent hostname: output changed from a bare `fetch failed` to `fetch failed: connect ECONNREFUSED 127.0.0.1:54321 [ECONNREFUSED]` and `fetch failed: getaddrinfo ENOTFOUND this-host-does-not-exist.invalid [ENOTFOUND]` respectively -- this is very likely the exact fix for what the user saw (their `fetch failed` almost certainly has a real cause -- wrong `AB_BASE_URL`, server not running/reachable, or a network/TLS issue -- that will now print). 5 new tests for `formatError` (plain Error, non-Error thrown value, cause with/without a `.code`, non-Error cause ignored).
5. **Testing-methodology fix, this time actually followed through on**: earlier rounds' snapshot-before/restore-after discipline still let the *real* `config.json` get corrupted a **third** time this session (same pty-bot-timing artifact class, reconfirmed via a scratch-file test that the underlying save/default logic is correct) because full-flow multi-question monte-carlo runs were still being pointed at the real file in between snapshots. Root-caused and fixed the process, not just the symptom again: `configure.ts` already has a `-f/--config PATH` flag for exactly this -- **any further live verification of `configure` must target a disposable scratch path via `-f`, never the real `config.json`, for anything beyond a single already-proven-safe question**. Reset `monteCarlo` to defaults a third time and confirmed via a `-f <scratch path> -s monte-carlo` run that the scratch file and the real file are completely independent (real file provably untouched by that run).
6. 222 tests total (5 new, all in `formatError`'s describe block -- the explain()/reordering changes are display-only, verified live rather than by additional unit assertions).

**Sixteenth round, same session: `dashboard` becomes its own config.json section, `AB_BIRTH_DATE` removed.**

1. **New `FireConfig.dashboard: DashboardConfig` section** (`{ birthDate, retirementAges, planToAge }`) in `fire-accounts.ts`, replacing three scattered top-level `FireConfig` fields -- matches the existing `crossover`/`monteCarlo` pattern (each `-s` section now maps to exactly one JSON property). New `DEFAULT_DASHBOARD_CONFIG` export mirrors `DEFAULT_CROSSOVER_CONFIG`/`DEFAULT_MONTE_CARLO_CONFIG`.
2. **`configure.ts`'s section renamed `"personal"` → `"plan"`** (not `"dashboard"`, deliberately -- `-d`/`--dashboard PATH` already means "the generated `fire-dashboard.json`," a completely different thing; reusing the word for a `-s` section value would be a real, confusing collision). `askBirthDate`/`askRetirementAges`/`askPlanToAge` now mutate `draft.dashboard.*`; `main()`'s draft-cloning extended to deep-clone `dashboard` (including its `retirementAges` array) alongside the existing `crossover`/`monteCarlo` clones.
3. **Real migration path, not just a schema rename**: `loadFireConfig` now reads `dashboard` if present, else falls back to the *original* flat top-level `birthDate`/`retirementAges`/`planToAge` fields (the shape this repo's own real `config.json` was still in at the time of this change -- confirmed live, not assumed). Read-compatible with the old shape; the very next write always produces the new nested one, no explicit migration step needed. Two new tests cover this directly (old-flat-shape migration, and new-`dashboard`-wins-over-stale-flat-fields precedence).
4. **`AB_BIRTH_DATE` removed entirely** (env var table, `-b` flag docs, the "missing birth date" error message, example invocations) -- `--birth-date`/`config.json`'s `dashboard.birthDate` are the only two sources now. The CLI-flag-overrides-config precedence for all three fields (`-r`/`-b`/`-p` in `reports-fire.ts`) was **already correct** before this round (`options.X ?? fireConfig.dashboard.X`) -- verified live rather than re-implemented: ran with the scratch config's stored birth date (1976-07-31 → age 50) then with `-b 1990-01-01` (→ age 36), confirming the override actually takes effect.
5. **Live-verified the real repo's actual pre-migration state**, not a synthetic fixture: copied the real `config.json` (still flat-shaped at the time) to a scratch path, loaded it directly via `loadFireConfig`, confirmed the real `birthDate`/`retirementAges: [55, 52, 59]`/`planToAge` all migrated correctly into `dashboard`; ran `reports fire --dry-run` and `configure -s plan` against that same scratch copy end-to-end, confirming the rewritten file now has the new nested shape with every real value intact. Real `config.json` itself was never touched this round (all verification used `-f <scratch path>`, continuing the process fix from round 15).
6. 224 tests total (2 new, both in `fire-accounts.test.ts`'s `loadFireConfig` describe block).

**Seventeenth round, same session: three small `configure` prompt-UX tweaks.**
1. Removed the `"Only asking: <sections>. Everything else keeps its current value in config.json."` line printed at the start of a narrowed (`-s ...`) run -- user found it unnecessary noise.
2. **Retirement ages collapsed from a multi-step loop** (one `promptNumber` per age plus a "Compare another retirement age too?" y/n after each) **into a single free-text line**: `Simulate retirement at ages [52, 55, 59]: `, accepting space- and/or comma-separated numbers in one answer (regex split on `/[,\s]+/`, filtered for empty tokens, `Number.isFinite` + `> 0` validated, re-prompts on anything invalid). Blank accepts the existing list; empty with no existing default (a bare `,`/whitespace-only or truly empty answer) re-prompts rather than being accepted, matching every other required-field prompt in this file. New `askRetirementAges` no longer uses `promptNumber`/`confirmOnTty` at all -- it's a standalone `tty.question` loop, the same style as `askBirthDate`.
3. **`askPlanToAge`'s prompt text shortened** from `"Assume the plan needs to last to this age (a conservative default, not a lifespan estimate)"` to just `"Simulate plan to age"` -- the longer explanatory clause was judged unnecessary given the field name alone is now considered self-explanatory (this file has no `explain()` line for plan-to-age either, before or after this change).
4. Verified live via a scratch config (never the real `config.json`, continuing the round-15 methodology fix): confirmed the "Only asking" line no longer prints for `-s plan`; confirmed blank-accepting the retirement-ages prompt preserves `[52, 55, 59]` unchanged; confirmed typing `"50, 62 70"` (mixed comma+space) correctly parses to `[50, 62, 70]` and persists that way in the written file; confirmed the new "Simulate plan to age" wording renders correctly.
5. No test file exists for `configure.ts` (thin CLI layer, same convention as every other executable-CLI file in this repo -- only the pure logic modules it calls into are unit-tested), so this round's verification is live-only, matching how every other `configure.ts` UX change this session has been checked. Test count unchanged at 224. README's "Plan questions" section gained a short example transcript showing the new one-line multi-age prompt.

**Eighteenth round, same session: Rule of 55 early-access strategy, the first of a planned multi-phase feature.**

Triggered by debugging a real user-reported disparity: the crossover widget said "you can retire now" while the Monte Carlo charts failed for nearly every pre-59 retirement age. Root cause, confirmed against Actual's own real `monteCarloSimulation.ts` engine source (not guessed): 84.6% of the user's real portfolio sits in accounts with the tool's own `accessAge: 59` default (four 401(k)s + a Roth IRA), and the engine has a cliff-edge failure rule -- the instant a year's *accessible* pots can't cover that year's spending, it zeroes the entire simulation, including still-locked pots, and marks it permanently failed from that year forward. Not a bug in this tool's generated JSON (verified directly); an accurate simulation of a real access-age gap the crossover widget doesn't model at all.

User wanted this turned into a real feature: know the actual early-access strategies (researched via `WebSearch`, since `WebFetch` on the user's suggested source, madfientist.com, returned HTTP 403) and auto-apply the ones that legitimately fit a given account. Planned and scoped with the user via `AskUserQuestion` before writing any code: **phased delivery, Rule of 55 only this round**; **auto-applied**, not a separate manually-toggled strategy picker; **SEPP/72(t) explicitly out of scope, permanently** (its fixed IRS-formula payment schedule has no honest mapping onto Actual's binary `accessAge` threshold -- approximating it would be actively misleading, so it's documented as unsupported rather than guessed at). The Roth conversion ladder was scoped as a future Phase 2, sketched but not designed (real open questions remain: Actual has no "conversion" concept at all, only static pots; a ladder would need to be approximated as N synthetic sub-pots with staggered access ages and zeroed withdrawal tax; how much to convert per rung and how that interacts with comparing multiple retirement ages at once are both unresolved).

**Rule of 55 (IRS Code §72(t)(2)(A)(v))**: separating from an employer during or after the calendar year you turn 55 allows penalty-free (not tax-free -- ordinary income tax still applies) withdrawals from *that employer's own* 401(k)/403(b), starting immediately. Two hard conditions verified via `WebSearch`, not assumed: it's employer-plan-only, lost entirely the moment the balance rolls into an IRA (this tool's `retirement-tax-deferred`/`retirement-roth` categories already conflate 401(k)s with IRAs -- a known prior simplification); and it's keyed to the calendar year of separation, not the exact birthday.

1. **New account-level fact, `ruleOf55SeparationAge: number | null`**, added to `ClassifiedAccount`/`FireAccountOverride` in `fire-accounts.ts`, same "no category-based default, override-only" pattern as `monthlyContribution`. Its mere presence *is* the "this is a real, currently-held employer plan" assertion -- no separate boolean needed. Threaded through all three `classifyAccounts()` branches (override/heuristic/default) and validated in `loadFireConfig` (must be a positive number when present, same pattern as every other numeric field).
2. **New `./actual configure` question**, gated to exactly the two categories that default to `accessAge: 59` (`retirement-tax-deferred`/`retirement-roth` -- HSA/taxable are already unrestricted, so the question would be meaningless there): an `explain()` block covering the real mechanics and the IRA/employer-plan distinction, then a free-text "Age you'll separate from this employer (0 if not applicable)" question, carried over as a default on re-runs same as every other per-account field, saved immediately after answering.
3. **New pure function `effectiveAccessAge(account)` in `fire-dashboard.ts`**: returns `account.accessAge` unchanged unless `ruleOf55SeparationAge >= 55` (the exception's own floor -- a separation age below 55 doesn't qualify at all), in which case it returns `Math.min(accessAge, ruleOf55SeparationAge)` (or just the separation age if `accessAge` is null). `buildPot` now calls this instead of using `account.accessAge` directly -- no new parameters needed anywhere upstream, since it's a pure function of already-classified per-account fields, applied identically across every retirement-age scenario (Rule of 55 is a fact about *when a job ends*, not about which retirement age is being simulated).
4. **`reports-fire.ts`** prints one line per qualifying account after the existing balance/spend summary lines: `Rule of 55 applied: Fidelity Workday 401k accessible from age 55 (was 59).` -- silently skipped when no account qualifies, comparing `effectiveAccessAge(account) !== account.accessAge`.
5. **8 new tests**: two `loadFireConfig` validation tests (rejects non-positive, accepts null) in `fire-accounts.test.ts`; one `buildPot` test plus a 5-case `effectiveAccessAge` describe block (no separation age; qualifying and earlier; below 55 has no effect; above the existing accessAge still takes the minimum; null accessAge uses the separation age directly) in `fire-dashboard.test.ts`. Also fixed two pre-existing tests' `account()`/expected-object literals that were missing the new required `ClassifiedAccount` field, caught immediately by `./actual build`'s type errors. 232 tests total.
6. **Live-verified against the user's real budget, using a scratch copy of the real `config.json`** (continuing the round-15 `-f <scratch path>` discipline -- never the real file for anything beyond a single already-proven-safe question). First asked the user directly which of their three 401(k)s (Workday/Sandia/F5) are current-employer plans rather than guessing from names -- confirmed only Workday qualifies, at a separation age of 55. Real gotcha caught mid-verification: an account's config override is looked up by **account id, not name** (`findOverride` matches `config.accounts[].match` against the account's UUID first) -- an initial grep for "workday" against the `match` field itself found nothing, which looked like "no override exists yet" but was actually searching the wrong field; the account already had a real override (for `allocationPreset`/`monthlyContribution`) that a *new, appended* override for the same account would never reach, since `findOverride` uses `.find()` and returns the first match. Fixed by editing the *existing* override in place rather than appending a duplicate. After that fix, the scratch-config dry run correctly showed the Workday pot's `accessAge: 55` (was 59) in the generated JSON across all three retirement-age scenarios, and printed the "Rule of 55 applied" console line.
7. **Applied the real answer to the user's actual `config.json`** with their explicit confirmation (`ruleOf55SeparationAge: 55` on the real Workday 401k override) -- `config.json` is gitignored (personal financial data), so this change is local-only and was never a git-tracked diff. Re-ran `./actual reports fire -n` against the real file afterward to confirm the same line prints for real.
8. README gained: a new paragraph in the account-classification section documenting the question, its exact wording, the real IRS mechanics (not "trust us"), and the effect on `accessAge`; the carry-over-on-re-run sentence extended to mention `ruleOf55SeparationAge` alongside the other per-account fields; a short "strategies not modeled" note (Roth conversion ladder, SEPP/72(t)) explaining *why* each is left out rather than leaving them unmentioned.

**Nineteenth round, same session: the generate/import/edit cycle diagnosed and replaced with a read-only `--check`, plus a real config-data-loss incident.**

Started as "how hard would a Firefox extension be" and became a redesign of the whole workflow. **User's framing, which turned out to be the right diagnosis**: most of what `configure` asks exists only to preserve values across regenerations, and would be unnecessary if the values simply lived in Actual. Confirmed against our own code -- `extractConfigFromDashboard` and the "picking up dashboard changes" prompt exist purely to paper over a conflict we create by owning fields Actual already owns (safe withdrawal rate, projection type, inflation, withdrawal rule, tax bands, sim count -- we literally copied Actual's own tooltip text for each).

1. **Investigated whether anything outside Actual's UI can write dashboards. It cannot.** `loot-core/src/server/api.ts` has 70 `api/*` handlers and zero touching dashboards/widgets/reports, so neither actual-server nor the actual-http-api wrapper this repo talks to can write them. The handlers exist (`dashboard-import`, `dashboard-add-widget`, `dashboard-update-widget`, ... in `loot-core/src/server/dashboard/app.ts`, all `mutator(undoable(...))` emitting CRDT sync messages) but run inside a Web/SharedWorker reached via a module-private `globalWorker` closure in `platform/client/connection/index.ts`. `window.Actual` (typed in `loot-core/typings/window.ts`) is a shell bridge only -- file dialogs, theme, updates, sync-server control, **no `send`**. So an extension's only routes are driving the UI's own import flow (coarse, replaces the whole page) or monkey-patching `Worker`/`SharedWorker` before boot (unsupported internals). Extension deferred on that basis; the higher-leverage move would be contributing `api/dashboard-*` upstream.
2. **Reading dashboards, on the other hand, is fully supported and was the unlock.** actual-http-api exposes `POST /budgets/{budgetSyncId}/run-query` (ActualQL), gated behind its experimental-operations flag (501 when off; verified enabled on the user's deployment). `dashboard` and `dashboard_pages` are first-class AQL tables, and the schema declares **`meta: f('json')`**, so every widget's whole configuration comes back already parsed. No browser, no scraping, no export file, same `x-api-key`. This is the single most reusable fact from this round.
3. **New direction, agreed with the user: stop being a generator, become a linter.** Scaffold once, then read the live dashboard and report. New `src/fire-analysis.ts` (pure): `simulateBridge` (deterministic mean-return projection applying the same accessible-only funding rule as Actual's engine, grossing withdrawals up for tax -- a *best* case, so a failure there fails in essentially every simulated run), `detectPotDrift` (live pot access ages vs. what config would generate), `detectCrossoverMismatch` (accounts the crossover counts vs. accounts actually simulated). Wired as `./actual reports fire -c/--check`, which writes nothing.
4. **The crossover widget is the only one holding a live category selection and date range**; the Monte Carlo widget has no category concept at all -- just a frozen `annualWithdrawal` we computed at generation time. `--check` now reads `expenseCategoryIds` + `timeFrame` off the live crossover and recomputes spend from them, so the user's own dashboard edits became the *input* instead of something regeneration destroys. Effect on real data was dramatic: $276,710/yr (all 82 categories, 12 months) → $142,987/yr (their 43 categories, 44 months), which turned "every pre-59 age fails" into "52 has a 2-year gap, 55 reaches 94, 59 funds to 100."
5. **Deriving contributions from transactions does NOT work -- tried it, reverted it.** These are brokerage-synced accounts whose transactions are fund-level churn (`exchangeIn`/`exchangeOut`/`realizedGainLoss`/`fees`/`Reconciliation balance adjustment`); summing them produced $619k/yr "contributions" on a $1.3M account. Worse, excluding transfers is backwards -- a real 401(k) contribution from checking often *is* a transfer while market noise is not. There is no reliable signal in this data. The configured `monthlyContribution` question stays.
6. **Real data-loss incident, found by accident and disclosed immediately.** The user's `config.json` had silently dropped 5 of 11 accounts (including the Rule of 55 setting from the previous round). Cause: `configure` did `draft.accounts = []` before the classification loop while saving after every question, so an interrupted run truncated the list to however far it got. **Fixed properly**: new `overrideIndexFor` (matches id OR name, mirroring `findOverride` -- matching by id alone would append a duplicate that `findOverride`'s `.find()` never reaches, a bug I hit myself against a scratch config earlier in the session) and `pruneStaleOverrides`, applied only *after* a completed pass. `draft.accounts` is now cloned from `existingConfig` and entries update in place. Verified end-to-end with a pty driver against scratch configs: a SIGKILL mid-run leaves all 11 entries intact, and a completed run still prunes a seeded stale entry (12 → 11).
7. **Lesson reinforced**: the round-15 rule (never live-test `configure` against the real `config.json`) is necessary but wasn't sufficient -- this loss happened outside our testing entirely. Any code that rewrites a whole user-owned list needs to be non-destructive under interruption by construction, not by discipline.
8. 262 tests total (25 new: bridge/drift/mismatch analysis, plus `overrideIndexFor`/`pruneStaleOverrides`). Still deliberately NOT done: shrinking the `configure` interview (dropping the ~20 passthrough questions), deleting `extractConfigFromDashboard`/`mergeGeneratedDashboard`, and the Roth conversion ladder.

**Twentieth round, same session: `configure`/`reports fire` replaced by a local companion web app, account types, and an IRS-limit "max" sentinel.**

Three small, related requests compounded into a full rearchitecture. First: the IRS contribution-limit prompt was unreadable, since a `retirement-tax-deferred` account might be a 401(k) or an IRA and the tool couldn't tell which, so it printed both limits hedged with "whichever this account actually is." Second: let the user classify accounts more precisely (401(k), Roth IRA, money market, ...) so Rule of 55 isn't asked on an IRA (where it can never apply) and a "max" contribution option becomes meaningful. Third, once account type was on the table: "let's just make `configure` a UI" (the CLI interview's remaining problems -- can't express "ask X only if Y," can't recompute a shared limit's remaining room as you go, can't jump back to fix one field -- are consequences of a linear interview being the wrong shape, not CLI polish) -- and immediately after, "fold `reports fire`/`--check` into the same UI too, no more CLI at all," followed by the real north star: this repo's destination is a **companion app for Actual**, retirement/FIRE first, with bulk budget edits and spending analysis (today's `set-values`/`anomalies`) as later phases.

Before writing any code, built and iterated a throwaway HTML/CSS/JS prototype (published as a Claude artifact, "Runway") to validate the design direction cheaply. First pass invented its own visual identity; user said "make the style more similar to actual-budget wherever possible" -- pulled Actual's own `DESIGN.md` and theme CSS straight from `actualbudget/actual` (not guessed): the real dark-theme palette (`gray900`/`gray800` page/card, navy-ink sidebar kept deliberately distinct from the page background, one purple accent used sparingly per their own stated "One Accent Rule"), Inter as the one typeface with tabular OpenType features for figures (not a separate mono face, which the first pass had wrongly used), 4px/6px radii, and flat cards with borders instead of shadows (their DESIGN.md is explicit that shadows are for transient overlays only). One deliberate, disclosed deviation: dimmed their literal full-strength purple card border, since applying it everywhere visibly exceeded their own "under 10% of the screen" rule. User also asked for dark-mode-only, which per the artifact-design skill means declaring the dark palette directly on bare `:root` with no light/media-query blocks at all, rather than a light-first design gated behind `prefers-color-scheme`.

**1. `AccountType` replaces category as the thing a person picks** (`fire-accounts.ts`): ten concrete types (`traditional-401k`, `roth-401k`, `traditional-ira`, `roth-ira`, `inherited-ira`, `hsa`, `brokerage`, `debt`, `cash`, `other`), each with a full `ACCOUNT_TYPE_TRAITS` entry -- `category` (still the same 7-value union every downstream consumer, e.g. `isPortfolioCategory`, keeps reading unchanged), `taxTreatment`, `accessAge`, `allocationPreset`, `ruleOf55Eligible` (true only for the two employer-plan types), `limitGroup` (`"employer-plan"`/`"ira"`/`"hsa"`/null -- which accounts share one IRS limit pool), `label`. **Real correctness fix, not just a UX one**: `inherited-ira` gets `accessAge: null`, not the usual 59 -- IRC §72(t)(2)(A)(iv) exempts inherited/beneficiary IRAs from the early-withdrawal penalty at any age, a well-established rule this tool had been asserting wrong for exactly one of the user's own 11 real accounts (`Fidelity IRA BDA`). Heuristic (`classifyByHeuristic`) now guesses an `AccountType` directly, gained a `bda`/`beneficiary`/`inherited` pattern (checked before the generic IRA pattern) and splits Roth/employer-plan/IRA combinations that used to collapse into one category.

2. **Migrating the existing `config.json`, done properly, not just sketched**: `FireAccountOverride.category` is now a legacy-only field; `classifyAccounts`'s override branch resolves a missing `type` via `guessTypeFromLegacyCategory(category, accountName)` -- deliberately done in `classifyAccounts`, not `loadFireConfig`, because a real override's `match` is usually an account **id**, not a name (confirmed against the user's own file), so only the classification step (which has the live account list) can actually use the name to disambiguate. **A real bug found and fixed during live verification, not just anticipated in the plan**: the old category-based `configure` had always written `taxTreatment`/`accessAge` out explicitly for every account (never left them to "inherit from category"), so a naive migration let the stale, pre-fix `accessAge: 59` mask the new `inherited-ira` type's correct `null` -- caught by curling the live server against a scratch copy of the real config and seeing `Fidelity IRA BDA` still show `accessAge: 59` after migration. Fixed by having `classifyAccounts` ignore a **legacy** override's `taxTreatment`/`accessAge` entirely (using the freshly guessed type's own defaults instead) while still honoring `allocationPreset`/`monthlyContribution`/`ruleOf55SeparationAge`, which were always genuine per-account answers, never blanket-written. A `type`-bearing (already-migrated) override's `taxTreatment`/`accessAge` remain real, honored overrides going forward. Verified live afterward: `Fidelity IRA BDA` → `inherited-ira`, `accessAge: null`; `E*Trade Roth IRA` → `roth-ira`; `Fidelity Workday 401k`'s Rule of 55 (`55`) and all contributions survived untouched.

3. **`monthlyContribution` gains a `"max"` sentinel**, stored as the literal string (never resolved into a number at rest, so it keeps re-resolving correctly as IRS limits update yearly and as the person crosses the 50/60-63 age tiers). New pure functions: `annualContributionLimit(limitGroup, age, irsLimits)` (the age-tiered annual limit, including the 60-63-vs-50-59-or-64+ employer-plan distinction), `resolveMonthlyContributions(overrides, birthDate, irsLimits)` (within each limit group, sums every *explicit* contribution first and gives the one `"max"` account, if any, the remainder -- exactly the sharing rule the user specified; missing birth date/IRS limits resolves `"max"` to nothing rather than throwing, matching this repo's existing "IRS limits are advisory, never required" stance). `classifyAccounts`/`loadClassifiedAccounts` gained optional `birthDate`/`irsLimits` parameters (both default `null`, so every pre-existing test call site needed zero changes) so `ClassifiedAccount.monthlyContribution` stays exactly what it always was -- an already-resolved `number | null` -- meaning `totalMonthlyContribution`, `buildContributions`, and every other existing consumer needed no changes at all.

4. **Precise IRS limit lines**, replacing the old hedged-both-limits prose: new `contributionLimitLines(type, irsLimits)` prints exactly the format the user asked for (`Roth IRA: $7500.00/yr [$625.00/mo]`, `Roth IRA age 50+: $8600.00/yr [$716.67/mo]`, etc.) -- one clear limit now that type is known, not two guessed ones.

5. **`config.json` shrinks**: `crossover`/`monteCarlo` sections dropped entirely (schema is now just `{ version, accounts, dashboard }`). This was low-risk specifically *because* `mergeGeneratedDashboard` already has a completely independent preservation mechanism -- it reads the *previously generated dashboard file*, not `config.json`, so nothing about "keep my tuned assumptions across regenerations" was lost. `fire-dashboard.ts` gained `DEFAULT_CROSSOVER_ASSUMPTIONS`/`DEFAULT_MONTE_CARLO_ASSUMPTIONS` (Actual's own real UI defaults, relocated from `fire-accounts.ts`, used only the very first time a dashboard is generated). `extractConfigFromDashboard` and its private helpers were deleted outright -- their only purpose (recovering crossover/Monte Carlo assumptions before a re-run's questions would overwrite them) stopped applying the moment those stopped being asked about or stored.

6. **No more CLI for this whole feature.** `src/configure.ts` and `src/reports-fire.ts` are deleted. Their logic split cleanly along a line that was already implicit: `src/fire-generate.ts` (new) holds `generateDashboard`/`checkDashboard` -- the exact same real-data-fetching and dashboard-building/analysis logic `reports-fire.ts`'s `main()`/`runCheck` had, just returning a structured result instead of calling `console.log`, making both directly unit-testable and reusable. `formatFinding`/`LEVEL_MARKERS` (console-only formatting in `fire-analysis.ts`) became dead code once nothing prints to a terminal, and were deleted along with their test. Dropped, not carried forward, since nobody asked for them and the new page makes the underlying action a normal editable field instead: `reports-fire.ts`'s old `-r`/`-b`/`-p` one-off overrides and `-n`/`--dry-run` preview.

7. **New `src/app-server.ts`**: a plain `node:http` server (no new dependency), routes namespaced under `/api/retirement/` specifically so a future `/api/budget/...` or `/api/transactions/...` section -- the other two companion-app phases -- is a new prefix later, not a rewrite. `GET /api/retirement/state` returns one JSON snapshot (plan config, every open account merged with live balance + classification + per-type metadata + IRS limit lines, the full `ACCOUNT_TYPES` table) that every mutating route also returns after persisting, so the client always renders from one consistent shape. `PATCH /api/retirement/plan`, `PATCH /api/retirement/accounts/:id` (creates an override if none exists yet, re-prunes stale overrides against the freshly fetched open-account list as a side effect -- mirrors the old `configure.ts`'s end-of-pass prune, just triggered by any edit instead of a completed CLI run), `POST /api/retirement/generate`, `GET /api/retirement/check`. New `src/app.ts` (replaces `configure.ts`) is a thin CLI bootstrap only -- parses `-f`/`-i`/`-o`/`-p`/`--no-open`, starts the server, best-effort opens a browser (`open`/`xdg-open`/`start`, swallowed non-fatally -- this sandbox has no GUI, so the printed URL is the real, required path here), blocks until Ctrl+C.

8. **New `src/app-ui/`** (plain HTML/CSS/vanilla JS, no framework, no build step): one page, a nav with exactly one working item today (**Retirement**) and two visibly "Planned" placeholders (Budget, Transactions) communicating the north star without building anything for them yet. *Configure* tab: Plan fields (autosave on change) and an Accounts list where each row's Type dropdown drives which fields show (Rule of 55 only for the two employer-plan types; allocation/contribution only for portfolio types; no contribution field at all for `inherited-ira`, enforced both client-side for display and server-side in `applyAccountPatch` as a real validation, not just a UI nicety) and a live "Max" toggle (client-side enforces at most one `"max"` per limit group, by clearing a sibling's max before setting a new one, using a `limitGroup` field the server exposes per type specifically so the client doesn't have to duplicate `fire-accounts.ts`'s own grouping logic). *Analyze* tab: a Generate button showing the structured result, and a Check section rendering `driftFindings`/`bridgeFindings` as color-coded fail/warn/info/ok chips.

9. **`./actual` dispatcher and `package.json`**: `configure`/`reports` removed entirely (the `reports` subcommand group had exactly one subcommand, `fire`, so the whole group went with it); one new `app` case/bin entry replaces both.

10. **Dead code removal**: `promptChoice`/`promptNumber`/`confirmOnTty` deleted from `actual-helpers.ts` along with their tests, confirmed (via grep) to have no other caller now that `configure.ts` is gone. `TtyInterface`/`openTtyInterface` were *not* deletable, despite the plan's initial claim -- `confirmViaTty` (still used by `set-budget.ts`) depends on them internally; caught by actually reading the code rather than trusting the plan's own earlier assumption.

11. **`src/app-server.test.ts`** (new): route-level tests against a real `node:http` server on an ephemeral port, hit with real `fetch()` calls, backed by a real temp `config.json`/`irs-limits.json` on disk (no `node:fs` mocking needed -- a temp file exercises `loadFireConfig`/`writeFireConfig` exactly as the real app does) and a mocked global `fetch` standing in for Actual's REST API. The mock only intercepts calls to the fake Actual host and passes local-loopback calls (to the test's own server) straight through to the real `fetch` -- both share one global, so failing to distinguish them would have broken either the server's outbound calls or the test's inbound ones. 284 tests total (this round added/updated roughly 40: `ACCOUNT_TYPE_TRAITS`/`annualContributionLimit`/`resolveMonthlyContributions`/`contributionLimitLines`/legacy-migration coverage in `fire-accounts.test.ts`, plus the new `app-server.test.ts`).

12. **Live-verified against the user's real budget on a scratch copy** (continuing this session's established `-f`/`-i`/`-o` discipline -- never the real files for anything beyond a single already-proven-safe step): `GET /api/retirement/state` correctly migrated all 11 real accounts (confirmed the two IRAs and the accessAge fix specifically), `POST /api/retirement/generate` wrote a real dashboard with the Rule of 55 boost applied, and `GET /api/retirement/check` correctly surfaced the inherited-IRA access-age fix itself as a live drift finding against the account's *previously imported* dashboard (`"Fidelity IRA BDA: dashboard has access age 59, config would generate none"`) -- the new correctness fix and the existing drift-detection feature reinforcing each other exactly as designed, not by coincidence. No forced bulk-migration script was needed for the user's real `config.json`: `loadFireConfig` is already read-compatible with the old category-only shape (same "read old, write new" pattern as the earlier `dashboard` flat-to-nested migration), so opening the real app and editing any one account naturally migrates it.

13. README fully rewritten: the `./actual configure`/`./actual reports fire` sections replaced by one `./actual app` section (the companion-app framing, the Configure/Analyze tabs, account types, the "max" sentinel and its exact limit-line format, the inherited-IRA correctness note, the migration note, "strategies not modeled"); Layout section's file list updated for `fire-analysis.ts`/`fire-generate.ts`/`app-server.ts`/`app-ui/`/`app.ts` and the deletions.

**Twenty-first round, same session: a real batch of UX/domain fixes to the new companion app -- layout, a genuine performance bug behind "Max feels delayed," a savings/HYSA type, employer 401(k) match against the combined IRC 415(c) limit, HSA self/family coverage, a Rule of 55 checkbox, and a mortgage payoff calculator.**

1. **Layout**: `main` was hard-capped at `max-width: 980px` regardless of the actual browser window width -- the literal cause of "a lot of unused horizontal space." Widened to 1360px, account rows went from a cramped `1.4fr 1fr` split to a fixed `260px` identity column + a 4-column fields grid (2 columns on medium widths, 1 on mobile), so short captions that used to wrap now mostly fit on one line.
2. **The real bug behind "Max feels delayed, and calculated the wrong value" -- two separate, both real, root causes found by testing rather than guessing:**
   - **Performance**: every single PATCH (any field, on any account) was calling `buildState`, which re-fetched **every account's full transaction history** just to redisplay balances that a config edit can't possibly change. Measured live: 3.6-5.4s for the user's 11 real accounts, every edit. Fixed with a per-process balance cache (`getBalances`, `"fresh"` for `GET /state`, `"cached"` for every mutating route, which only fetches an account this process has never seen before) -- measured after the fix: ~45-54ms per edit, a two-orders-of-magnitude improvement. Also added a client-side in-flight lock (`runExclusive`) plus a "Saving…" pill, since the "Max" toggle is really two sequential requests (clear a sibling, then set this one) and an impatient second click during a slow window could otherwise interleave a second pair of requests.
   - **A genuine calculation bug**, found only once the performance fix let live testing actually complete fast enough to isolate it: `resolveMonthlyContributions` guessed a legacy (category-only) override's type from `override.match` -- which is usually an account **id**, not a name -- instead of the account's real name. A legacy Roth IRA's UUID matches neither the IRA nor employer-plan name pattern, so it silently fell through to `roth-401k`'s default, grouping it into the employer-plan limit ($24,500+) instead of the IRA limit ($7,500) and returning the wrong "max" figure. This bug predated this session's whole account-type rearchitecture (the underlying call pattern was there from the start) but had never been exercised against a *legacy* override with a *real* UUID-shaped `match` in a test before. Fixed by giving `resolveMonthlyContributions` an optional real-accounts parameter (used to look up the true name for legacy-type-guessing, same fix pattern `classifyAccounts` itself already used) -- confirmed live against the user's real Roth IRA: now resolves to $716.67/mo, matching the IRA limit, not the employer-plan one. A live-caught, self-inflicted false alarm along the way: an early re-test showed the bug still present because the *scratch* `irs-limits.json` copy was stale (missing the new `annualAdditions` field below, which the tightened `loadIrsLimits` validation now requires) -- `loadIrsLimits` was silently returning `null` for the whole file, masking the real fix until the scratch copy was refreshed from the real one.
3. **New `savings` account type** (high-yield savings account / money market): taxable like a brokerage, no age-based access restriction, but defaults to the `cash` allocation preset instead of `equity-80` -- retroactively replaces the earlier round's manual workaround (reclassifying the user's real "Ally Money Market" as `brokerage` + an explicit `cash` allocationPreset override). Heuristic and legacy-category migration both recognize "money market"/"HYSA"/"high-yield savings" by name, deliberately **not** a bare "savings" (too likely to be an ordinary cash buffer, not portfolio). The user's real Money Market account now classifies as `savings` automatically, no config edit needed.
4. **Employer 401(k) match, against the real, separately-verified IRC §415(c) "annual additions" limit** (not guessed -- confirmed via `WebSearch`: **$72,000 base for 2026**, with the *same* $8,000/$11,250 catch-up dollar amounts as the elective-deferral limit applying on top, a real IRS quirk, not an assumption). New `irs-limits.json` field `employerPlan.annualAdditions`; `combinedAnnualAdditionsLimit(age, irsLimits)` mirrors the existing age-tiering logic. New per-account `annualSalary`/`employerMatchRate`/`employerMatchCapRate` fields model a flat two-number "match% up to cap% of pay" formula (deliberately not a tiered formula like "100% on the first 3%, 50% on the next 2%" -- the user was asked and chose this shape over a flat-dollar or free-text-only alternative). `computeEmployerContribution`/`employerContributionSummary` estimate the employer's contribution and flag (not silently cap) when employee+employer together would exceed the combined limit -- deliberately a warning, not automatic capping, since which side "should" reduce first is genuinely ambiguous and plan-dependent.
5. **HSA self/family coverage**: new per-account `hsaCoverage: "self" | "family"` (defaults to `"self"`, the smaller/safer assumption). `annualContributionLimit`/`contributionLimitLines` both gained an `hsaCoverage` parameter, replacing the old "always show the self-only figure" behavior -- the shown lines and a "Max" contribution now both use whichever coverage is actually selected for that account.
6. **Rule of 55 UI**: the "0 for not applicable" number field (kept for backward compatibility as the underlying stored value) is now presented as a checkbox ("Active account with this employer") that reveals an age input defaulting to 55 when checked, rather than a bare 0 meaning "never asked" and a real age meaning "asked and answered" with no visual distinction between the two.
7. **New mortgage/loan payoff calculator** (`calculateMortgagePayoff` in `fire-analysis.ts`, shown for any `debt`-type account, not literally named "mortgage"): four inputs (interest rate, monthly payment, a balance, and the date that balance is *as of* -- deliberately independent of Actual's own ledger balance for the account, since a real servicer's payoff balance often isn't what a synced/manually-tracked Actual account reflects) produce a payoff date via the standard fixed-payment amortization formula, or a clear error ("payment doesn't cover the interest") instead of a nonsensical result when the loan would never actually pay down. New `addMonthsToDate` in `actual-helpers.ts` (month-precise date math, alongside the existing day-precise `addDays`) powers the date arithmetic.
8. **Binds to all interfaces, not just loopback** (a separate, smaller request answered the same round): `startAppServer` now listens on `0.0.0.0`; `RunningServer.networkUrls` lists every non-internal IPv4 address so the app can be reached from another device on the same network (e.g. a home server, viewed from a phone). Printed clearly at startup alongside an explicit, un-hedged note that there is no authentication at all -- a real, meaningfully widened blast radius, documented rather than left implicit.
9. 306 tests total (this round added ~35: `combinedAnnualAdditionsLimit`, `computeEmployerContribution`, `employerContributionSummary`, HSA-coverage variants of `annualContributionLimit`/`contributionLimitLines`, the legacy-override-id regression guard for `resolveMonthlyContributions`, `calculateMortgagePayoff`, plus `app-server.test.ts` coverage for the network-binding change).
10. README's Retirement/Configure-tab section extended for the savings type, employer match + combined-limit, HSA coverage, the Rule of 55 checkbox, and the mortgage payoff calculator; the `irs-limits.json` example and its surrounding prose updated for `annualAdditions` (the earlier round's now-stale "family coverage isn't used for Max" caveat removed, since it's implemented now).
